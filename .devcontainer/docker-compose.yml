version: '3'
services:
  playground:
    container_name: 'dotnet'
    hostname: 'dotnet'
    build: .
    restart: always
    working_dir: '/opt'
    tty: true
    volumes:
      - type: bind
        source: ../opt
        target: /opt
